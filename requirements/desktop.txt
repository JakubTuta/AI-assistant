pyautogui>=0.9
pygetwindow>=0.0.9
pyperclip>=1.8
# click_text screenshots the display to find the words to click. Not in the
# module's Requirement: without it every other desktop job still works.
mss
